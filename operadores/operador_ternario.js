const hora = 17;

//cpnstante      expressão    valor 1 true  valor 2 false
const saudaçao = hora <= 11 ? 'Bom dia!' : hora <= 17 ? 'Boa tarde!' : 'Boa noite'

console.log(saudaçao);
{
const saudaçao = 
    hora <= 11 
        ? "Bom dia!" 
            : hora <= 17 
             ? "Boa tarde" 
             : "Boa noite";
}
console.log(saudaçao);

